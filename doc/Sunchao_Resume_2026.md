# 孙超

**男** · 39 岁 · 16 年工作经验  
**电话** 18514509695 · **邮箱** 623569510@qq.com  
**求职意向** 后端架构 / 高性能网关 / 分布式架构 · **期望城市** 北京

---

## 个人优势

- **消息触达与 IM 架构**：十余年高并发实时消息系统经验。在乐视主导 **用户消息触达**（Golang + C fan-out、百万同时在线），并负责 **用户等级异步计算**（Kafka + Go 消费行为日志、日亿级）；精通长连接触达、Redis 路由与按 key 有序消费。

- **一线互联网落地**：具备新浪、乐视、优信等头部企业架构与研发经验，擅长从 0 到 1 搭建高可用消息、实时触达与数据处理系统。

- **IM 全链路能力**：在每日健康 **从 0 自研医患 IM**（私聊、群聊、踢人、禁言、会话管理等），独立完成协议、业务与后端落地；技术栈以 **Golang** 为主，兼顾 **C** 高性能网络层与 Java/PHP 多栈协作。

- **云原生与可观测**：精通 Docker、K8s、HPA、CI/CD；擅长传统长连接服务容器化改造、弹性调度，以及 Prometheus / Grafana 全链路监控体系建设。

- **压测与稳定性**：具备完整压测、容灾演练与性能优化经验，擅长处理长连接风暴、流量突峰、消息乱序、热点击穿等典型线上问题。

---

## 专业技能

| 方向 | 能力要点 |
|------|----------|
| **长连接 & 网络** | Golang / C 高性能服务；WebSocket、TCP；Reactor、epoll；万级长连接接入与调优 |
| **消息 & IM** | 私聊 / 群聊 / 推送触达；fan-out 多段路由；Protobuf；Redis 在线拓扑与限流；**自研 RTMQ 进程总线**（publish / async_send） |
| **分布式 & 流式** | Kafka 消费与 topic 分区；按 user_id / 业务 key 保证有序；Redis + MySQL 异步落库；Consumer 水平扩展 |
| **框架 & 通信** | Gin / Kratos / go-zero、gRPC、Swoole；RPC、MQ、缓存中间件 |
| **存储 & 检索** | MySQL 优化、Elasticsearch 海量检索 |
| **云原生 & 运维** | Docker、K8s、HPA、CI/CD；Prometheus、Grafana、Loki |
| **基础** | Linux 调优、Shell、Git、数据结构与算法、AC 自动机、常用设计模式 |

---

## 工作经历

### 每日健康 · Golang 后端 · 2019.06 – 2026.02

公司从事糖尿病慢病管理，通过动态血糖仪、运动监测等设备，结合客户端、医生端、SCRM 医患管理系统、小程序等为用户提供监测、打卡分析、用药方案等服务。本人在技术部负责各端后端支撑。

**业绩与职责**

- 参与公司技术选型与架构设计，完成业务体系从 0 到 1 搭建；基于 gRPC 完成基础服务能力开发。
- **自研医患 IM 消息系统**（Golang）：面向患者与内部工作人员 App 内沟通，覆盖 **私聊、群聊、踢人、禁言、成员管理** 等完整 IM 能力；独立负责消息协议、会话模型、后端接口与线上维护（用户规模精准可控，侧重 **IM 领域建模与可扩展架构**）。
- 负责客户端、医生端接口开发，动态血糖仪接入，以及 SCRM 系统搭建与核心接口维护。
- 推动业务服务容器化上 K8s，结合 Git + Jenkins 搭建 CI/CD，支撑敏捷迭代。

---

### 优舫北京信息科技有限公司（优信二手车金融中心）· PHP · 2017.06 – 2019.05

负责贷中、贷后业务及与银行资方对接的贷款申请、产品维护等工作。

**业绩**

- 负责贷中贷后接口开发维护，完成与银行资方联调，保障业务线上无重大故障。
- 主导将 ERP 状态机流程改造为 **Kafka 事件流**，完成系统解耦。
- 基于 Swoole 优化慢接口；将 ERP 检索改造为 **ES 检索**，查询由秒级优化至毫秒级。

---

### 乐视控股（北京）有限公司 · 后端架构工程师 · 2015.07 – 2017.03

#### 核心项目一：用户消息触达平台（Golang + C · 长连接 push + 离线 inbox）

**职责**

- 作为核心负责人，主导 **全站用户消息触达** 架构设计与研发：区分 **在线长连接 push** 与 **离线分表 inbox**，结合用户画像与在线状态做投放策略，支撑乐视 **千万级用户、百万级同时在线** 场景。
- 主导触达链路 **2.0 演进**：在线用户由「分表异步写库 + 拉取」升级为 **接入层 fan-out + 业务分片 + 异步广播**；与离线通道并行，实现在线段 **秒级～分钟级** 全量触达收尾。
- 搭建全站统一用户消息中台，覆盖系统消息、活动消息、评论消息等，支撑多业务线快速接入。

**业绩**

- 触达平台 **日均千万级 PV**，架构解耦、扩展性强，线上长期稳定，零重大事故。
- 同等灰度规模下，**在线段全量触达收尾由约 10 分钟级缩短至约 1 分钟内**；架构支持 **百万级同时在线** 水平扩展（接入层按连接数线性扩容）。

#### 核心项目二：用户等级异步计算（Golang + Kafka · 行为日志算分）

**职责**

- 负责 **用户积分 / 成长等级** 计算链路的设计与实现：播放等行为埋点写入 **Kafka**，由 **Go 消费服务** 异步处理，支撑全站 **日亿级** 行为数据。
- 按 **user_id 分区路由**（hash 取模进 Kafka partition），保证 **同一用户行为串行计算**，避免多实例并发导致积分/等级脏写。
- 消费侧 **批量拉取、批量提交 offset**；积分/等级先写 **Redis**，再 **异步同步 MySQL**；配合日志与消费位点监控保障稳定运行。
- 参与 **用户画像** 相关大数据项目，负责业务侧数据对接与跨地域协作。

**业绩**

- 等级计算链路 **线上长期稳定运行**，**日处理上亿条** 用户行为数据。
- 用户画像相关子项 **提前交付**，支撑上层运营与大数据策略。

---

### 新浪网技术（中国）有限公司 · PHP · 2013.08 – 2015.07

- 负责新浪博客消息、通知、SASS、提醒、纸条等基础服务接口开发维护及项目迁移。
- 负责博客广告分成、「亮瞎眼」APP 等服务端开发与维护。

**业绩**：所负责基础服务长期稳定运行；完成 SASS 迁移与 APP 接口按期交付。

---

### 春藤网络 · Java · 2010.07 – 2013.07

移动运营商 SP 服务商，负责积分活动、奖品发放、移动机器人等业务研发。

**业绩**：营销活动按时交付、运行稳定；持续重构优化，提升团队效率。

---

## 个人开源 / 技术实战

### 长连接 IM 架构研究与群聊链路验证（Beehive-IM · Golang + C + Docker）

**项目简介**  
基于乐视触达与 IM 同源架构，对 **接入层 / frwder / RTMQ 进程总线 / usrsvr·msgsvr 业务层** 进行源码级精读与 Demo 验证：完成 HTTP 注册调度、WebSocket 长连接、**群聊双段 fan-out（gid→nid→cid）**、Redis 在线拓扑等完整链路；Docker Compose 一键部署，配套 Web 双窗口 Demo、smoke 与 **RTMQ 单机压测脚本**。

**技术栈**  
Golang、C（frwder / RTMQ Server）、WebSocket、Protobuf、Redis、MongoDB、Docker Compose

**亮点**

- 梳理 **RTMQ 双 API**：`publish(cmd)` 业务进程广播订阅、`async_send(nid)` 指定接入单播；frwder **28888/28889 双平面** 桥接接入与业务。
- 单机压测（256B 小包）：publish 路径可持续 **~14 万 msg/s** 接收吞吐（内存转发、无持久化）；明确与 **Kafka 日志型 MQ 分工**（在线信令 vs 持久化流平台）。
- 梳理并验证 **usrsvr（群生命周期）+ msgsvr（群消息 fan-out）** 分工与 Redis 键设计。
- 实现/修复群聊 Demo 端到端（建群、加群、跨接入节点互发、退群解散）。
- 输出中文源码导读、RTMQ 设计文档与学习计划，支撑面试口述与代码 walkthrough。

---

## 教育经历

**商丘职业技术学院** · 计算机信息管理 · 大专 · 2006 – 2009

---

## 附：乐视等级系统 · 简历口径 vs 设计稿（自用，勿贴 PDF）

| 维度 | **简历 / 面试（实际上线）** | **异步近实时分布式处理系统.txt（完整设计，待 PoC 复现）** |
|------|---------------------------|--------------------------------------------------------|
| Kafka + Go 消费 | ✅ | ✅ |
| user_id 分区、同用户串行 | ✅ | ✅ |
| Redis + MySQL 异步落库 | ✅ | ✅ |
| 日亿级行为 | ✅ | ✅ |
| Master 管控 / etcd | ❌ 未写进简历 | 设计有 |
| DLQ、死信重放 | ❌ | 设计有 |
| 16 partition / 4 Worker / 3.5 万 QPS | ❌ | 容量估算 |
| 近实时（秒～分钟） | 简历写 **异步**，不强调 SLA | 设计目标 |

**面试**：先讲 **已做** 的四条；被追问扩展时再说「分区数、管控面、DLQ 在方案里，与 Beehive 方向二 Kafka 同一套路，后续会 PoC 复现」。详见 [乐视用户等级系统-面试稿.md](乐视用户等级系统-面试稿.md)（口述稿需按本表 **收敛** 后再背）。

---

## 附：每日健康 IM 写进简历的利弊（自用，勿贴 PDF）

| 维度 | 判断 |
|------|------|
| **加分** | 证明 IM **从 0 到 1 ownership**（私聊/群聊/踢人/禁言），与乐视「大规模触达」形成 **大流量 + 全功能 IM** 互补；投 IM / 医患 SaaS / 消息平台岗 **明显加分**。 |
| **减分风险** | 若写成「高并发千万用户 IM」会 **被追问穿帮**；用户量小本身 **不减分**，夸大才减分。 |
| **写法原则** | 强调 **能力完整性** 与 **自研**，不写 DAU；用「精准医患场景 / 万级以下 / 全链路负责」定调。 |

**综合结论**：**加分项**，前提是 **诚实定量级 + 突出 IM 设计**，与乐视触达项目 **组合叙事** 最强。

---

## 附：面试口述稿（RTMQ + 群聊 + 触达 · 自用）

### 30 秒版（开场 / 「介绍一下 RTMQ」）

> 必嗨 IM 里各进程不直连，走自研 **RTMQ 进程总线**：业务进程按 **cmd 订阅**，下行按 **接入节点 NID 单播**。frwder 开 **28888 接入 / 28889 业务** 两个平面，上行 publish、下行 async_send。群聊是 **msgsvr 按 Redis gid→nid 第一段 fan-out**，websocket **ImGroup 第二段 fan-out**。RTMQ 是 **内存、低延迟、不持久化**，和 Kafka **分工不同**——我们压过单机小包 **约 14 万 msg/s** 转发，适合在线信令，不替代日志 MQ。

### 60 秒版（深挖 RTMQ 设计）

> **为什么要有 RTMQ**：websocket、msgsvr、usrsvr 如果 N×M 直连 TCP，扩容和运维都扛不住；RTMQ 让每个进程只连一个 Proxy，中心 Server 做路由。  
> **两条 API**：`publish(type)` 查订阅表，所有 SUB 了该 cmd 的业务进程都收到——比如上行 GROUP-CHAT 到 msgsvr；`async_send(nid)` 只送到指定接入 NID——比如 msgsvr fan-out 到 websocket:8002。  
> **和 Kafka 边界**：Kafka 是持久化日志 + 消费位点，适合数仓、审计、削峰；RTMQ 无磁盘、允许队列背压丢包，换 **亚毫秒～毫秒** 进程间转发。面试里我不会说「全面替代 Kafka」，而是 **IM 在线链路专用总线**。  
> **我的落地**：Beehive 源码精读 + Docker 群聊 Demo + `rtmq-bench` 压测；乐视触达是同源的 **接入 fan-out + 离线 inbox 双通道**，规模在千万用户、百万在线。

### 防追问三答

| 问题 | 答 |
|------|-----|
| RTMQ SUB 是用户还是进程？ | **Proxy 进程** TCP 连上后自动 SUB；用户 WebSocket 用 **GROUP-JOIN / ROOM-JOIN**，不是 CMD_SUB。 |
| 群消息怎么走 RTMQ？ | 上行 publish 到 msgsvr；msgsvr 对每个 nid **async_send**；websocket 再 **ImGroup** 推本机连接。 |
| RTMQ 挂了怎么办？ | 单点 Hub，无持久化；生产靠监控重启 + 业务幂等；离线靠 inbox / 拉取，不靠 RTMQ 回溯。 |

### 与简历三条如何串

```text
乐视触达（规模 + fan-out 架构）
    ↓ 同源
Beehive RTMQ + 群聊 Demo（设计可讲 + 数字可证）
    ↓ 互补
每日健康 IM（从 0 业务建模：群/禁言/踢人）
```
