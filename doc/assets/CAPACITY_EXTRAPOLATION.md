# 容量外推一页纸（面试填数用）

> 把 **本地 L1/L2/L3** 实测填进 **「实测」列**；**「百万外推」列** 自动给面试官看。

---

## 表 1：百万 **同时在线**（长连接）

| 指标 | 实测（64G K8s） | 百万外推 |
|------|-----------------|----------|
| 每 ws Pod 稳定连接 `C_pod` | _____（L1） | 需 **1,000,000 / C_pod** 个 Pod |
| ws Pod 副本数（实测时） | _____ | 例：C_pod=5000 → **200 Pod** |
| 每 Pod 内存（连接态） | _____ MB | 200 Pod × 8G ≈ **1.6 TB** 接入内存量级 |
| chatroom 副本 | _____ | 百万在线 **低消息率** 时 **5～20** 通常够 |

**结论句**：「本机标定 **每 Pod _____ 连接**；百万在线 **约 _____ 台 16C ws**；与必嗨 **NID 水平扩展** 一致。」

---

## 表 2：百万用户 **收一条公告**（1000 房 × 1000 人）

| 指标 | 实测（64G） | 外推到 100 万用户 |
|------|-------------|-------------------|
| 实测拓扑 | _____ rid × _____ 人/房 | **1000 × 1000** |
| 总连接（实测） | _____ | 1,000,000 |
| 单房 BC fan-out 耗时 | _____ s（L2/L3） | 同架构，并行 push |
| 每 Pod fan-out 能力 `D_pod`/s | _____（L2） | 总能力 = D_pod × ws数 |
| 100 房 push 总耗时 | _____ s | 1000 房 ≈ **×10**（或并行更短） |

**结论句**：「实测 **_____ 房 _____ 人** **_____ 秒** 发完；外推 1000 房 **约 _____ 秒～_____ 分钟**（非 MySQL 灌库小时级）。」

---

## 表 3：fan-out 公式（必写白板）

```text
总下行/s  ≈  Ingress QPS  ×  同房在线人数

百万公告总条数  =  1,000,000（每人 1 条）
峰值 ws 能力      =  D_pod × ws_Pod数
```

---

## 表 4：成本（给面试官「要多少机器/多少钱」）

| 场景 | 节点规模（按表 1） | 一日云成本（量级） |
|------|-------------------|-------------------|
| 面试 demo（本地） | 64G 单 VM | **¥0** |
| 一次性云上打点 | 2～3×16C × **4h** spot | **¥50～100** |
| 百万 **真连接** 24h | ~200×16C ws + 配套 | **¥1～3 万/天**（见 MILLION_DEMO_PLAN） |
| 百万 **在线 + 低消息率** 生产 | 200 ws + 10 chatroom + Redis 集群 | **包月谈价**，非面试必讲 |

---

*打印或投屏此页；**只填实测列**，外推列面试口述即可。*
